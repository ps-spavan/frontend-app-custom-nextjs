# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
language: "en-US"
early_access: false
reviews:
  path_instructions:
    - path: "**/*.ts"
      instructions: |
        Flag any variables that are declared but never read.
        Review all standard lint errors from @eslint/eslintrc.
chat:
  auto_reply: true
